% Generated by CIF to mCRL2.

% Sorts for CIF enumerations.
sort a2'LPE' = struct a2'loc1' | a2'loc2';

% Actions for CIF events.
act a2'e';

% Actions for CIF variables having certain values.
act a2''varvalue: a2'LPE';
act m'v''varvalue: Int;

% Action for CIF marker predicate.
act marked;

% Process for behavior of the CIF specification.
proc P(
    a2': a2'LPE',
    m'v': Int
) =
    % CIF linearized edges.
    ((a2' == a2'loc1')) -> a2'e' . P(a2' = a2'loc2')
    +
    % CIF variable value actions.
    a2''varvalue(a2') . P()
    +
    m'v''varvalue(m'v') . P()
    +
    % CIF 'marked' action.
    ((((m'v' == 3) && ((m'v' == 4) && (m'v' == 1))) && (((m'v' == 2) && (m'v' == 5)) && ((m'v' == 6) && (((a2' == a2'loc1') && ((m'v' == 7) && (m'v' == 8))) || ((a2' == a2'loc2') && ((m'v' == 9) && (m'v' == 10)))))))) -> marked . P()
;

% Initialization.
init P(a2'loc1', 0);
