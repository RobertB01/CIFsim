% Generated by CIF to mCRL2.

% Actions for CIF events.
act chan';

% Actions for CIF variables having certain values.
act recv1'v''varvalue: Int;
act recv2'v''varvalue: Int;

% Action for CIF marker predicate.
act marked;

% Process for behavior of the CIF specification.
proc P(
    recv1'v': Int,
    recv2'v': Int
) =
    % CIF linearized edges.
    (-2147483648 <= 1 && 1 <= 2147483647) -> chan' . P(recv1'v' = 1)
    +
    (-2147483648 <= 1 && 1 <= 2147483647) -> chan' . P(recv2'v' = 1)
    +
    (-2147483648 <= 2 && 2 <= 2147483647) -> chan' . P(recv1'v' = 2)
    +
    (-2147483648 <= 2 && 2 <= 2147483647) -> chan' . P(recv2'v' = 2)
    +
    % CIF variable value actions.
    recv1'v''varvalue(recv1'v') . P()
    +
    recv2'v''varvalue(recv2'v') . P()
    +
    % CIF 'marked' action.
    (false) -> marked . P()
;

% Initialization.
init P(0, 0);
